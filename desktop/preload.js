'use strict'

// Minimal preload — renderer is sandboxed with contextIsolation.
// Extend this file if you ever need to expose Node/Electron APIs
// to the renderer via contextBridge.exposeInMainWorld().
